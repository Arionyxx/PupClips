# Supabase Configuration
# Get these values from your Supabase project settings at https://app.supabase.com

# Required: Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Required: Your Supabase anonymous/public key (safe to use in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Service role key for server-side operations
# WARNING: This key has admin privileges. NEVER expose it to the client!
# Only use in server-side code (API routes, server components, server actions)
# Keep this key secure and do not commit it to version control
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
